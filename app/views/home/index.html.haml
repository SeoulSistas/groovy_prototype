- if user_signed_in?
- else
  =link_to 'Sign In', new_session_path(:user)
  </br>
  </br>
  %h1 Welcome To Groovy
  </br>
  .row.text-center
    =image_tag("logo.png")
  </br>
  </br>

.question_new
  =link_to 'Ask a question', new_question_path, data: { no_turbolink: true }
%h2 Questions Near You
- if @questions.blank?
  .message There are no questions :(
- else
  .homequestions
    - for question in @questions
      .hquestion
        .homequestion_content=link_to question.content, question_path(question)
        .homequestion_location
          Near
          =question.location
          %br
          =question.answers.count
          Answers
          %hr

#panel
  %input#address{:type => "textbox", :value => "Stanford, CA"}
  %input{:type => "submit", :value => "Go!", :onclick => "filterCodeAddress()"}
  %br

%br
#filter-map-canvas

#all_questions
