- if not questions.empty?
  .questions
    - for question in questions
      .question_cntr
        .question.left
          .question_content= question.content
          .num_answers
            =question.answers.count
            Answers
        .ttl.right
          .question_ttl= question.display_ttl
- else
  .message No questions found 
